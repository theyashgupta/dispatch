# Design Contract — Console Grade

**Console Grade** is Dispatch's chrome treated as an instrument panel for live agent sessions:
every piece of on-screen text and every container has an explicit rank and is styled to occupy
it. Density stays tight wherever scanning matters — the board, cards, and the sync strip keep
their current tightness because that density is load-bearing for the at-a-glance requirement —
while the two surfaces a person actually reads, the detail panel body and modal body, get more
typographic room. This document is the single source of visual direction for the current
milestone; every later UI pass derives from it and this document wins on any conflict.

**SUPERSEDED BY v3.4: the `### Density scale (v3.4)` table under `## Spacing rhythm` now fixes
the board's scan-surface density in px, so this paragraph's "keep their current tightness" clause
no longer describes the contract.** The reading-surface half of this paragraph is unchanged.

## References

| Product          | What it does that Dispatch does not                                                                                                                                                                    |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Linear           | Ships a disciplined 3-value radius vocabulary tied to element role rather than one flat radius everywhere, and reserves its accent color for exactly three jobs, never letting it drift into a fourth. |
| Raycast          | Uses a distinct 500 (Medium) weight as a real typographic step between regular and semibold, and its radius genuinely scales with element size instead of being purely decorative.                     |
| Warp             | Treats monospace as a separate typographic voice with its own sizing rule, not merely "the same copy in a different font."                                                                             |
| Vercel dashboard | Runs a compact top strip rather than a tall, marketing-scale header, keeping chrome subordinate to content.                                                                                            |

Arc, Superhuman, and Things are cited only as lower-confidence directional color for the accent
discipline and terse-surface feel — their exact numeric values were not independently verified
and are not the source of any fixed value below.

The Linear, Raycast, and Warp numbers referenced above come from published token extractions of
their public marketing sites, not their authenticated product UIs, so they are directional, not
literal targets. Every fixed value in this contract is Dispatch's own, checked against Dispatch's
own rendered content — never copied from these products.

## Type scale

| Token or rule                                            | Value                   | Checked at                                | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| -------------------------------------------------------- | ----------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Panel title — `--font-heading`                           | 17px                    | >=1600px, 1024-1599px, 768-1023px, <768px | Research started at 15px (the token's current shipped size); landed at 17px, held clean with no second-line wrap against a 410-character fixture title at every breakpoint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Modal heading — `modal-heading`                          | 17px                    | >=1600px, 1024-1599px, 768-1023px, <768px | Renders `--font-heading` at `--weight-semibold` in `--font-mono` at `--text`, defined once in `Modal.tsx`'s `headingStyle` — so a dialog's own title is never quieter than a card title behind it. Deferred decision 11 is resolved: the mono one-step-down sizing rule does NOT apply to a dialog title, because a dialog title has no adjacent UI text to sit below. The cascade reaches all eight `Modal.Header` call sites (the seven modals plus the nested `PlaybookDeleteConfirm` inside `SettingsModal.tsx`), confirmed identical at every site by Phase 88's own measurement (`88-BASELINE.md`'s `## Heading cascade audit`). Open Question 1 (a 60+ character playbook name at the 358px-wide dialog): measured PASS — the heading wraps to multiple lines with zero horizontal overflow and the close button never displaced; no global truncation was needed or added (see `88-BASELINE.md`'s `## Open Question 1`). |
| Sync-strip wordmark / first-run title — `--font-display` | 22px                    | >=1600px, 1024-1599px, 768-1023px         | Research started at 20px; landed at 22px, no visible clipping inside the strip at any breakpoint that renders the wordmark. Scoped to >=768px plus the first-run and startup-error titles: the strip does not render the wordmark below 768px at all (see the `wordmark` row under `## Chrome dimensions` and `docs/ARCHITECTURE.md#app-shell-zones`), so 22px has no sync-strip subject there. The first-run title is unaffected by the breakpoint and stays 22px at every width.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Card timestamps and column count chips — `--font-micro`  | 11px                    | >=1600px, 1024-1599px, 768-1023px, <768px | New token. Today these render at 12px (`--font-label`). Landed at 11px, confirmed legible at the narrowest breakpoint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Card title, description body — `--font-body`             | 13px                    | >=1600px, 1024-1599px, 768-1023px, <768px | Held, unchanged. The 410-character fixture title truncates to one line via the existing card CSS at every breakpoint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `type-scale-hold`                                        | 13px / 12px / 11px held | >=1600px, 1024-1599px, 768-1023px, <768px | v3.4 density confirmation. `--font-body` (13px), `--font-label` (12px) and `--font-micro` (11px) hold unchanged under the landed density (`card-padding` 6px/8px, `inter-card-gap` 6px, `column-header-height` 28px) at every breakpoint: card title `fontSize` 13px, column header `fontSize` 12px, count chip `fontSize` 11px, read live. The 288-char fixture title never overflows its card (`scrollWidth <= clientWidth`). Density came from spacing and line-height per the `### Density scale (v3.4)` rows below, not from smaller text, the locked decision this row records.                                                                                                                                                                                                                                                                                                                                            |

## Weight

| Token or rule                                                                                    | Value | Checked at                                | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------ | ----- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| New middle tier — column headers, panel section labels, sync-strip status text — `weight-medium` | 500   | >=1600px, 1024-1599px, 768-1023px, <768px | New token. The system font (macOS `-apple-system` / San Francisco) renders a genuine Medium weight rather than synthesizing one — confirmed via computed style reporting `500` verbatim, not snapped to 400 or 600. Implementation note: a computed-style DOM match (fontWeight 600 + fontSize 12px) is not a safe way to find these elements — it also matches card identifiers and the `Linear` source badge, which must stay at 600. The real implementation needs a dedicated class or component-level application, not a style query. |

`--weight-regular` (400, body text default) and `--weight-semibold` (600, card identifiers,
badges, and anything else deliberately bold today) are unchanged.

## Monospace

| Token or rule                    | Value     | Checked at                                | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------------- | --------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Sizing rule — `mono-sizing-rule` | 12 → 11px | >=1600px, 1024-1599px, 768-1023px, <768px | Mono has no scale of its own today; this is the rule that gives it one: "mono renders one step below its adjacent UI text size." Checked on the card identifier and the panel-header `Field mono` identifier (12→11px both), and independently on the modal heading's ORIGINAL 12px baseline (also 12→11px), checked separately from the `--font-heading` cascade above so the two questions weren't conflated. Whether the modal heading's NEW 17px size also needs a one-step-down mono treatment was not checked this phase — see `## Deferred decisions`. Phase 89 extended the rule to its third subject, the `Notice` primitive's mono block: both variants (the panel's scrolling `startError` block and the board card's clamped one) measured 12px against 12px `--font-label` neighbours at all four breakpoints and now take `--font-micro`, fixed once in `Notice.tsx` rather than at any of its five mono call sites. The rule is NOT yet established across every mono surface: twelve further call sites declare `--font-mono` beside `--font-label`, and whether each diverges depends on its own adjacent UI text size, which needs a live per-surface reading rather than a source census. **Phase 116 audit (F-01):** found and fixed a 13th call site the Phase 89 census missed, `PrList.tsx`'s PR-number badge (a detail-panel surface, in this milestone's scope), live-measured at 12px beside its own 12px `--font-label` sibling; landed at `--font-micro` (11px), live-confirmed before (12px) and after (11px). **Phase 116 code review (F-01 addendum):** a 14th in-scope call site surfaced post-close, `PrBadge.tsx`'s mono repo tag, which mounts on board surfaces (`CardView.tsx`, `GroupPrRow.tsx`) whenever a card's PRs span more than one repo; the 116-02 fixture's single-repo PR set never rendered it (a fixture blind spot), and it inherited the badge button's 12px `--font-label`. Fixed the same way (`--font-micro`), live-confirmed via a temporary multi-repo probe: before 12px, after 11px, badge height byte-identical (16.796875px) before and after. The twelve modal-adjacent call sites Phase 89's F-21 named remain outside board/detail scope and unmeasured this phase. |

Monospace's role: a semantic signal for identifiers, playbook names, and machine output — never
decoration.

## Spacing rhythm

| Token or rule                                                                                      | Value     | Checked at                                | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| -------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Detail-panel body and modal body padding — `reading-surface-padding`                               | 24px 16px | >=1600px, 1024-1599px, 768-1023px, <768px | Research sketched a two-container form (`padding: var(--space-xl) 0` plus a separate horizontal inset); what was actually checked and lands instead is a single-property asymmetric override, applied identically to the panel's scroll region and to both a sparse (New ticket) and a dense, tabbed (Settings) modal. Read cleanly with no overflow or cramping at every breakpoint, including the 358px-wide dialog at the narrowest breakpoint.                                                                                                                                                                                                                                                                                                                                                                                                 |
| Panel section rhythm - `--panel-section-gap`                                                       | 16px      | >=1600px, 1024-1599px, 768-1023px, <768px | Landed by Phase 115, replacing `ReferenceBlocks.tsx`, `PrList.tsx`, `CardTimeline.tsx` and `Notice.tsx`'s own ad hoc one-sided margins with one shared token, applied once as a flex-column `gap` at both `DetailPanel.tsx` reading-surface wrappers. Every raw gap value measured byte-identical to the pre-change baseline at every breakpoint (16px); the change is architectural, not a numeric retune: BEFORE, the uniform 16px was an accident of this fixture's own block combination (CSS's max-of-adjoining-margins collapse, with only one zero-margin block that never sat between two zero-margin neighbours); AFTER, the same 16px is produced by one token applied once, so the rhythm holds even for block combinations this fixture does not exercise. Break-proven via `scripts/panel-115.mjs`'s `CHECKS.rhythm`/`BREAKS.rhythm`. |
| Scan-surface padding (card padding, column inner padding, inter-card gap) — `scan-surface-padding` | unchanged | >=1600px, 1024-1599px, 768-1023px, <768px | Proof row, not a new value: the same seeded card's rendered height was measured before and after the reading-surface change and was identical at every breakpoint — the reading-surface rule does not leak into scan surfaces. **SUPERSEDED BY v3.4: the `### Density scale (v3.4)` table below now fixes `card-padding`, `inter-card-gap`, `row-rhythm` and `column-header-height` in px; "unchanged" no longer describes this row's scan surfaces.**                                                                                                                                                                                                                                                                                                                                                                                             |

The rule that changes: reading surfaces (detail panel body, modal body) get asymmetric padding
with more vertical rhythm than horizontal; scan surfaces (board, cards, sync strip) keep symmetric
tight padding exactly as today. The underlying 4/8/16/24/32/48 spacing scale itself is unchanged —
`--space-xs` (4px) through `--space-3xl` (48px) — only the rule for how reading surfaces use it
changes.

Reading surfaces also get slightly looser line spacing, which is the other half of "read rather than
scanned":

| Token or rule                                        | Value | Checked at                                | Note                                                                                                                                                                                                                       |
| ---------------------------------------------------- | ----- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Body line-height on reading surfaces — `--line-body` | 1.6   | >=1600px, 1024-1599px, 768-1023px, <768px | Up from 1.5, and only inside the detail-panel and modal body scroll regions. The token is unchanged everywhere else, so scan surfaces keep their current tightness. Checked against real multi-paragraph description copy. |

### Density scale (v3.4)

| Token or rule          | Value                                                                  | Checked at                                | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------- | ---------------------------------------------------------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `card-padding`         | 6px vertical / 8px horizontal (done-compact 6px flat)                  | >=1600px, 1024-1599px, 768-1023px, <768px | TRUE starting baseline was 4px vertical / 8px horizontal (`--space-xs` / `--space-sm`, `CardView.tsx:210-212`), with the compact `done` variant at 4px flat. CONTEXT.md's "start from the current 8px, test 6-7px vertical candidates" named `--space-sm`, the HORIZONTAL half of the shorthand only, not the true 4px vertical baseline. Landing 6px vertical is therefore a LOOSENING of that 4px baseline (+4px of card height from the padding alone; the padding-only arithmetic once quoted here, 75.6875px at BP-A/B and 104.296875px at BP-C/D from the 71.6875px / 100.296875px baselines, is the planning-stage artifact `row-rhythm`'s KI-03 note below retires: the measured landed heights are 75.03125px at BP-A/B and 103.640625px at BP-C/D once the concurrent line-height reduction is included), not the tightening CONTEXT.md's wrong-baseline framing implied. The done-compact variant lands at 6px flat, matching the new vertical value the same way. See `row-rhythm` below for the net pitch effect once combined with `inter-card-gap`.                                                                                                                                                                                                                                                                                             |
| `inter-card-gap`       | 6px                                                                    | >=1600px, 1024-1599px, 768-1023px, <768px | True baseline was 8px (`--space-sm`, `Column.tsx:405`). A real tightening of that baseline: the `done` column's live-read visible-card count went from 4 of 5 to 5 of 5 at BP-C, and stayed 5 of 5 at BP-A, BP-B and BP-D.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `row-rhythm`           | pitch = card content height + 18px                                     | >=1600px, 1024-1599px, 768-1023px, <768px | Arithmetic: (+4px padding, 2 x 2px) minus (-2px gap) = +2px net pitch, LOOSER than today at every breakpoint (79.6875px to 81.6875px at BP-A/B, 108.296875px to 110.296875px at BP-C/D), recorded honestly rather than as the tightening CONTEXT.md's wrong-baseline framing implied. `inter-card-gap`'s own isolated tightening is real but is absorbed by the combined padding increase once both land together. **Phase 116 audit reconciliation (KI-03):** the card-content-height figures elsewhere in this milestone's own interfaces blocks (75.6875px BP-A/B, 104.296875px BP-C/D) predicted a title-wrap-driven split for `PROP-401` that never occurs, its title is single-line and never wraps at any breakpoint, confirmed by two independent live sessions (114-03, 116-02). The measured truth is 75.03125px at BP-A/B (71.6875px baseline, +4px padding, minus 0.65625px from the 19.5px to 18.85px line-height reduction, net +3.34375px, matching exactly) and 103.640625px at BP-C/D, the latter driven by a DIFFERENT mechanism, the carousel layout's own structural "Move to..." trigger row, not title wrapping. Both measured values sit a consistent ~0.66px below the predicted figures; the predicted figures were a planning-stage rounding artifact, not a measurement, and are retired in favor of the two measured values above. |
| `column-header-height` | 28px flat at every breakpoint (no responsive rule)                     | >=1600px, 1024-1599px, 768-1023px, <768px | Down from 32px (`--column-header-height`, `Column.tsx:320`). Header height read exactly 28px at all four breakpoints; the count-chip's own rendered height stayed byte-identical (15.390625px) before and after, so the chip is not squeezed. CONTEXT.md's keep condition, no legibility loss below 768px, is met cleanly at BP-D, the real iPhone-12-Pro-metrics device preset.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `scan-line-height`     | global `:root --line-body: 1.45` (`.reading-surface` unchanged at 1.6) | >=1600px, 1024-1599px, 768-1023px, <768px | Down from 1.5. Card-title (scan surface) computed lineHeight moved 19.5px to 18.85px (2-line title height 39px to 37.6875px) at every breakpoint; a 1.4 leg was also clean but not chosen since 1.45 already delivers uniform tightening with more legibility margin. **NEW-19** (`scripts/check-invariants.mjs`) bars any `--line-body:` redefinition or `.reading-surface` use inside `src/web/features/board/`, so Phase 114 MUST implement this as a change to `tokens.css`'s global `:root --line-body` value (1.5 to 1.45); this is the one exception to the `--line-body` row above staying untouched, since this row records the SAME token's new global value rather than a board-local override. `.reading-surface` stays at 1.6 unchanged; a board-local override would trip `npm run invariants`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |

## Radius

| Token or rule                                                             | Value                | Checked at                                | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------- | -------------------- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Small — chips, badges — `--radius-sm`                                     | 4px                  | >=1600px, 1024-1599px, 768-1023px, <768px | Held, matches the research starting value. A census of every rendered element whose computed radius equals this token, taken at all four breakpoints in both board and Orca views, finds members from 15.39px to 24px: the board column count chips at 15.39px, the Orca section count chips at 16.50px, the inbox count badge and the PR, preview and session-lost badges at 16.8px, the `Linear` source badge and priority/status chips at 18.8px, and the two view-switch segments at 24px. This supersedes an earlier note claiming a 16.8–18.8px span with nothing between 18.8px and 28px — both halves were stale, the floor because the 11px count chips postdate it and the ceiling because the view-switch segments this same document records as "24×28px" are themselves in the bucket. The role-based rule below (up to roughly 19–27px) already covers the full measured span; only this row's height reading was wrong.                                                                |
| Workhorse — buttons, inputs, cards, columns — `--radius`                  | 6px                  | >=1600px, 1024-1599px, 768-1023px, <768px | Held, unchanged. This bucket spans roughly 28px (`IconButton`) to ~120px (seeded cards) tall — a wide, contiguous middle tier.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Large — modal dialog, the search dropdown, `MoveToPicker` — `--radius-lg` | 10px                 | >=1600px, 1024-1599px, 768-1023px, <768px | Held, matches the research starting value. An 8px alternative was tried at the widest breakpoint (where "soft" would show first against the dark surface) and did not read better; 10px stands. Phase 86 correction: the search dropdown and `MoveToPicker` moved into this bucket from the workhorse row — both are dismissible floating menus that share the modal's `--shadow-float` elevation treatment, so their radius now matches that elevation class rather than a raw height reading that under-measured the dropdown's typical multi-row height. Both independently clear the ~200px threshold too: `MoveToPicker`'s realistic height is 228px (5 rows × 44px + 8px gap) and the listbox's `maxHeight` is 360px. The modal dialog remains the bucket's tallest member (measured 630–909px tall depending on breakpoint) — the detail panel's outer shell has no visible edge to round today.                                                                                               |
| Excluded surfaces — `radius-scope`                                        | excluded — no radius | >=1600px, 1024-1599px, 768-1023px, <768px | Resize handles stay undecorated, as today. **The scrollbar thumb does not, and the "as today" half of this row was wrong when it was written.** Phase 89 measured `::-webkit-scrollbar-thumb` on a genuinely scrolling container (`scrollHeight > clientHeight` true) and read `border-radius: 6px`, from `tokens.css`'s app-wide `*::-webkit-scrollbar-thumb { border-radius: var(--radius); }` — a rule that predates this contract. The row is amended rather than the CSS, deliberately: the declaration sits inside the hard-won scrollbar block this document names as untouched, its value is inherited from the workhorse token rather than assigned to the thumb by the role-based rule below, and stripping a 6px radius off a 10px-wide thumb would be authoring a new visual decision, which an audit has no mandate to do. What the exclusion means in practice is that the vocabulary does not REACH the thumb — no rule below assigns it a bucket — not that the thumb renders square. |

The vocabulary is tied to element ROLE, expressed as a height threshold a reader can apply: an
element up to roughly 19–27px tall (a chip or badge) takes `--radius-sm`; an element from there up
to roughly 200px tall (buttons, inputs, cards, columns) takes the workhorse `--radius`; an element
200px tall or taller, or a dismissible floating menu sharing the modal's `--shadow-float`
elevation treatment (the modal dialog, the search dropdown, `MoveToPicker`), takes `--radius-lg`.
The vocabulary deliberately does not reach resize handles or the scrollbar thumb — radius there is
decoration with no hierarchy payoff, and the existing scroll-gutter and scrollbar handling in
`tokens.css` is hard-won and is not touched. Not reaching them is not the same as their rendering
square: the thumb inherits `var(--radius)` from that untouched block and measures 6px, as the
`radius-scope` row above records. (The line range this paragraph used to cite for the scroll-gutter
block went stale as `tokens.css` grew; the block is cited by name now so it cannot go stale again.)

## Color roles

| Token or rule                                                                   | Value                                                                                                                                                                                                                                                                                                                                 | Checked at                                | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Dominant background — `--bg`                                                    | app background, error and first-run screens; unchanged                                                                                                                                                                                                                                                                                | >=1600px, 1024-1599px, 768-1023px, <768px | The darkest baseline the surface ladder rises from.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Elevation ladder — `--surface-column`, `--surface-card`, `--surface-card-hover` | column / card / card-hover surface tiers; unchanged                                                                                                                                                                                                                                                                                   | >=1600px, 1024-1599px, 768-1023px, <768px | Rank via lightness, no shadows at rest — see `## Elevation`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Hairline dividers — `--border`                                                  | borders and dividers; unchanged                                                                                                                                                                                                                                                                                                       | >=1600px, 1024-1599px, 768-1023px, <768px | Separates without competing for attention.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Accent — `--accent`                                                             | interactive/selected-state signal only: active view toggle, active inbox badge, drag-target highlight, needs-attention card border, resize-handle hover, active search result row, active session-switcher segment, active Claude account badge (header popover and Settings, Accounts) and the sign-in link in the add-account modal | >=1600px, 1024-1599px, 768-1023px, <768px | Job, one sentence: the accent means "this is the interactive or selected thing," and only that. Keyboard focus has moved off the accent onto its own outline treatment below (`Button.tsx:98`, `IconButton.tsx:42`). **The collision this row used to name does not exist and, on the evidence, never did in the shipped code.** Phase 89 measured both rings live: needs-attention resolves `boxShadow: rgb(94, 106, 210) 0 0 0 1px` with a matching `borderColor` — `--accent` — at all four breakpoints, while multi-selected resolves `rgb(232, 233, 234)`, which is `--text`. Two different colours. `CardView.tsx` also composes them in an `if (needsAttention) … else if (multiSelected)` chain, so a single card can never carry both rings whatever their colours; the collision was structurally impossible as well as factually absent. Reproduction note, because the trigger is easy to get wrong: `needsAttention` is NOT "sits in the Needs Input column" — `card-attention.ts` returns true for a non-null `startError`, for `sessionLost` outside the done column, or for a non-empty `cleanupBlocked`, and a plain `needs_input` card reads `boxShadow: none`. **`KEEP-04` finding, Phase 96:** `SessionSwitcher.tsx`'s active session pill was a new `--accent` consumer this job list did not name — flagged as a candidate by `96-01-SUMMARY.md`, adjudicated here by `panel-96.mjs` (live: active pill `color` computes to the same `--accent` value as the token itself, `rgb(94, 106, 210)`, at all four breakpoints). The row was under-specified, not violated: the pill's `background: color-mix(in srgb, var(--accent) 16%, var(--surface-column))` on the active segment plus `var(--accent)` foreground is the IDENTICAL formula this same table's `view-switch` pattern and the inbox count badge already use for "the accent marks the active segment of a segmented, single-active-choice control" — the same job, a new instance of it, not a fourth one. The job list is extended to name it explicitly rather than left to be re-derived by the next reader. |
| Destructive - `--destructive` (fill) / `--destructive-text` (text)              | sync-failure / disconnected / config-error signal; `--destructive` covers fills, stripes, borders, icon glyphs and status dots at the 3:1 non-text floor, `--destructive-text` covers every literal text usage at the 4.5:1 text floor                                                                                                | >=1600px, 1024-1599px, 768-1023px, <768px | Fill/text role split landed by Phase 115, see `## Contrast floor` and `## Deferred decisions` row 12. `--destructive-text` is `#ea6d71`, a white 20% lighten of `--destructive`, holding the same hue/saturation (358deg, 75%) so neither reads as a second semantic color. 17 text call sites repointed to `--destructive-text`; 5 non-text call sites (chip fill, icon glyphs, the event-tint map, the CI-fail status dot) stay on the unchanged `--destructive` fill token, and the danger `Button` background moved to its own `--destructive-button-fill` (`#c03136`, Phase 115 review fix WR-01) so its white label clears the 4.5:1 text floor, see `contrast-danger-button-label` under `## Contrast floor`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Text — `--text`, `--text-muted`                                                 | primary text / muted text (identifiers, headers, counts, timestamps); unchanged                                                                                                                                                                                                                                                       | >=1600px, 1024-1599px, 768-1023px, <768px |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Keyboard focus — `focus-ring`                                                   | outline: 2px solid var(--accent); outline-offset: 2px                                                                                                                                                                                                                                                                                 | >=1600px, 1024-1599px, 768-1023px, <768px | Replaces `boxShadow: "0 0 0 2px var(--accent)"`, which was structurally identical to selection. The rule: **a keyboard focus ring must never look identical to selection.** This rule is authored here and lands in `frontend-design-system.md` during Phase 84, alongside the primitive changes that implement it. Confirmed keyboard-only via real Tab traversal and a real pointer click that leaves no ring, at every breakpoint. One rendering nuance, not blocking, **with its mechanism corrected in Phase 89**: at the narrowest breakpoint the first card in a column loses 1px of its ring on the top, left and right edges. The cause is not the sticky header — Phase 89 measured `headerBottom === listTop === cardTop` at both the collapsed 20.7969px and the shipped 32px header height, so the header does not overlay the list at all. The first card fills the list's width and sits flush at its top, so the 1px `outline: auto` ring is clipped at the scroll container's own boundary, which merely coincides with the header's bottom edge. The condition is identical at both header heights (obscured delta 0.00px on all four sides), predates this contract, and the bottom edge stays fully inside the client rect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Semantic palettes — `--prio-*`, `--col-*`, `--status-*`                         | untouched                                                                                                                                                                                                                                                                                                                             | >=1600px, 1024-1599px, 768-1023px, <768px | Already correctly separated from the accent and from each other; out of scope for this narrowing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## State palette

Hover, pressed and focus-visible values for every interactive board element, transcribed from the
phase ledger. Three rules the table below implements: hover is a background-tier change only, no
transform and no shadow; the pressed state is one `color-mix` formula applied from each element's
own resting tier, never a per-element value; and focus-visible keeps the v2.9 `focus-ring`
treatment (see `## Color roles`) and extends its coverage, so this section amends that row's reach
without changing the ring itself. **The `focus-ring` row in `## Color roles` stays in force.**

| Token or rule               | Value                                                                                                                                                                    | Checked at                                                                 | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `hover-card`                | `--surface-card-hover` (#202126, unchanged token), landed with `boxShadow: none` and `transform: none` forced                                                            | >=1600px, 1024-1599px, 768-1023px, <768px                                  | **CardView.tsx currently violates the background-tier-only rule**: real hover today also sets `boxShadow: rgba(0,0,0,0.3) 0px 2px 8px 0px` via an unconditional push in `boxShadowParts` around `CardView.tsx:186-188`; Phase 114 must delete that branch. Tighter and wider `color-mix` alternatives were tried and rejected, no measurement showed the current tier failing to separate a hovered card from resting siblings. Does not affect the closed `--destructive-text` contrast-floor row (`## Contrast floor`); the fill token, `--destructive`, is unchanged. At <768px this is a rendered-state check, the candidate is applied programmatically since the device preset has no hover input.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `hover-column-header`       | no hover token, the header is not interactive                                                                                                                            | >=1600px, 1024-1599px, 768-1023px, <768px                                  | `Column.tsx:319` onward: no click handler, no hover-driven style branch. A real hover produces byte-identical computed background before and after. If a later phase makes the header interactive, come back to this row rather than assign a hover value ad hoc. At <768px this is a rendered-state check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `hover-resize-handle`       | `2px solid color-mix(in srgb, var(--accent) 55%, transparent)`                                                                                                           | >=1600px, 1024-1599px only, not rendered at 768-1023px or <768px, see Note | Today, hover, keyboard focus and active dragging all resolve to the identical opaque `2px solid var(--accent)` (`hoveringHandle \|\| resizing \|\| handleFocused`, one expression). The landed value is a lower-weight translucent accent, distinct from the opaque value the next row keeps for focus and dragging. The handle does not render at all at 768-1023px or <768px, `Board.tsx`'s carousel query removes its entire JSX subtree below 1024px, a structural finding beyond a hover-value gap. Phase 115 ported this exact three-tier pattern into `DetailPanel.tsx`'s own resize handle, previously missing a hover tier entirely (hover and pressed collapsed onto the identical solid `--accent`, with no CSS transition); confirmed live and break-proven by `scripts/panel-115.mjs`'s `CHECKS.states`/`BREAKS.states`, see `115-MEASUREMENTS.md`'s `## States` section. Milestone-close integration check (2026-09-01): the board handle's own hover colour change was still instant while every sibling hover fades at 80ms; `--resize-handle-transition` now names both border-left-color and border-right-color so the one token governs both handles, and `Column.tsx`'s handle consumes it.                                                                                                                                                                                               |
| `hover-icon-button`         | `--surface-card-hover` (confirmed, unchanged)                                                                                                                            | >=1600px, 1024-1599px, 768-1023px, <768px                                  | Tested on the always-mounted Settings ("Sync filters") gear (`IconButton.tsx:42-43`). Resting transparent to hovered #202126, no `boxShadow`, no `transform`. At <768px this is a rendered-state check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `hover-button-secondary`    | `--surface-card-hover` (confirmed, unchanged)                                                                                                                            | >=1600px, 1024-1599px, 768-1023px, <768px                                  | Tested via a runtime replica built from `Button.tsx:92-98`'s own `secondaryStyle` object, since no live secondary-variant instance is reachable on the seeded board. At <768px this is a rendered-state check on the injected replica.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `hover-button-primary`      | `color-mix(in srgb, white 12%, var(--accent))`, resolving #717cd7                                                                                                        | >=1600px, 1024-1599px, 768-1023px, <768px                                  | **No hover feedback today**: `Button.tsx:88-98`'s `primary` branch keeps `base.background` unconditionally on hover. New surface, not a retune. A lighten step, matching the neutral-surface hover direction, derived from the variant's own resting background, tested via a runtime replica. Phase 114 must add a `primary`-branch hover case to `Button.tsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `hover-button-danger`       | `color-mix(in srgb, white 12%, var(--destructive-button-fill))`, resolving #c84a4e                                                                                       | >=1600px, 1024-1599px, 768-1023px, <768px                                  | **No hover feedback today**, same reason as `hover-button-primary`. Same lighten formula and percentage, for consistency across both solid accent-colored variants. Phase 114 must add a `danger`-branch hover case to `Button.tsx`. Rebased from `var(--destructive)` (then resolving #e85e62) onto the darker `--destructive-button-fill` in the Phase 115 review fix (WR-01), keeping the white label at 4.62:1 on hover, above the 4.5:1 text floor.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `hover-new-ticket`          | `--surface-card-hover` (confirmed, unchanged)                                                                                                                            | >=1600px, 1024-1599px, 768-1023px, <768px                                  | `SyncStrip.tsx:258-277`'s own `newTicketHovered` state. Confirmed correct as shipped. At <768px this is a rendered-state check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `hover-view-switch-segment` | inactive segment: `--surface-card-hover` (confirmed); active segment: **no observable hover**                                                                            | >=1600px, 1024-1599px, 768-1023px, <768px                                  | **Real, measured code defect, not a retune target.** `IconButton.tsx`'s style-merge order (`...iconButtonStyle, background: hovered ? ... : "transparent", ...focusRing(focused), ...style`) lets the active segment's own `style.background` tint always win, so hovering the active segment produces zero visible change. The inactive segment hovers correctly, matching the shared token. Phase 114 must change the merge order or add a dedicated hovered-and-active composition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `hover-transition`          | 80ms `ease-out` `background-color`                                                                                                                                       | >=1600px, 1024-1599px, 768-1023px, <768px                                  | **SUPERSEDED BY v3.4: replaces the v2.9 `## Motion` rule that "nothing animates on hover."** Baseline was zero (`transitionDuration` read `0s`). Judged clean via a real pointer sweep across adjacent cards at >=1600px and 1024-1599px; 60ms read abrupt, 100ms (the ceiling) started reading laggy. 768-1023px and <768px are rendered-state checks. Confirmed under emulated `prefers-reduced-motion: reduce`: `transitionDuration` reads `0s`, the global kill-switch reaches it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `pressed-formula`           | `color-mix(in srgb, black 12%, <element's own resting background>)`                                                                                                      | >=1600px, 1024-1599px, 768-1023px, <768px                                  | No `:active` or pressed state exists anywhere in `src/web/` today, new surface not a retune. One formula covers both solid tiers (opaque elements darken their own hex) and `transparent` tiers (translucent black overlay). Computed at 12%: `--surface-card` #1a1b1f to #17181b; `--surface-card-hover` #202126 to #1c1d21; `--surface-column` #131417 to #111214. Tested on 4 elements incl. the resize handle at >=1600px/1024-1599px, 3 elements (no resize handle) at 768-1023px/<768px, rendered-state application since no `:active` handler exists to dispatch a real mousedown against. Re-measured through `scripts/contrast-113.mjs --extra-bg`: `--destructive-text` (the text-role token Phase 115 landed, see `## Contrast floor`) on pressed-`--surface-card-hover` (#1c1d21) now measures 5.55:1, clear of the 4.5:1 floor; the former 4.30:1 pressed-tier residual is closed at the value level (`## Deferred decisions` row 12). See the elevation-ladder rule under `## Elevation`: the pressed state does not enter that ladder.                                                                                                                                                                                                                                                                                                                                                         |
| `focus-visible-coverage`    | every interactive element reachable by keyboard, treated with `focusRing()`'s governed `2px solid var(--accent)` ring, never rendering identically to hover or selection | >=1600px, 1024-1599px, 768-1023px, <768px                                  | `IconButton`/`Button` secondary/primary/danger/New-ticket/view-switch segments and `SearchBox` already consume `focusRing()`, no gap. Column header: not focusable, correct. **Gap 1**: `CardView.tsx` carries no `tabIndex`, but `Card.tsx` spreads `useDraggable()`'s default `attributes` (`role="button"`, `tabIndex={0}`) onto every card, so it IS keyboard-focusable today and renders the browser's ungoverned native outline, never `focusRing()`, a worse gap than "not focusable." **Gap 2**: the resize handle's hover, keyboard focus and active-dragging states render via the identical `hoveringHandle \|\| resizing \|\| handleFocused` expression (`Column.tsx:314`) and never call `focusRing()`; the landed `hover-resize-handle` value distinguishes hover but focus and dragging remain visually identical to each other. **Both gaps CLOSED by Phase 114** (Deferred decisions rows 13 and 14): `Card.tsx` now composes its focus handler onto `focusRing()`'s governed ring, and `Column.tsx`'s resize handle now calls `focusRing(handleFocused, true)` for a flush treatment distinct from its own border-based hover/dragging state; both confirmed live by `node scripts/panel-114.mjs --check board-states`. >=1600px and 1024-1599px used a real 14-press Tab traversal plus a real pointer click; 768-1023px and <768px are rendered-state checks reading `tabIndex` directly. |

## Elevation

| Token or rule                                     | Value                                                                                                                                                   | Checked at                                          | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Float shadow — `--shadow-float`                   | 0 6px 16px rgba(0,0,0,0.45)                                                                                                                             | >=1600px, 1024-1599px, 768-1023px, <768px           | Formalizes the existing copy-pasted literal (already shipped on the search dropdown) as a named token. A tighter `0 4px 12px` alternative was tried at the widest and narrowest breakpoints and did not read as an improvement against the dark surfaces; the current literal stands.                                                                                                                                                                                                                                                                                                                                                 |
| `elevation-ladder`                                | strictly increasing relative luminance: 0.00366 / 0.00701 / 0.01102 / 0.01535 (`--bg` / `--surface-column` / `--surface-card` / `--surface-card-hover`) | n/a, viewport-independent; computed from tokens.css | Re-verified by `scripts/contrast-113.mjs`, break-proven, a mutated copy with `--surface-card` darker than `--surface-column` trips the check. Confirms the ladder's lightness-rank rule holds with no exception; the single-shadow-token invariant is unrelated and already gated by `scripts/check-invariants.mjs`. Phase 115 additionally verified the ladder live on rendered surfaces, panel and board, at all four breakpoints via `scripts/panel-115.mjs`'s break-proven `CHECKS.elevation`, not only computed from `tokens.css`.                                                                                               |
| Terminal recessed tier - `terminal-recessed-tier` | `--bg`, recessed below `--surface-column`, ACCEPTED EXCEPTION                                                                                           | n/a, viewport-independent; computed from tokens.css | The terminal viewport sits on the base `--bg` background tier deliberately, recessed below the panel's own `--surface-column` tier, because it is a distinct, self-contained reading surface: its own connecting-state and error-state text renders at 6.02:1 (`--text-muted`) and 6.45:1 (`--destructive-text`) against `--bg`, both well clear of the 4.5:1 floor, and its live-session state delegates entirely to xterm.js's own, separately-verified theme. This is the one deliberate exception to the ladder's rank ordering, verified by measurement in `115-MEASUREMENTS.md`'s `## Elevation` section, not a silent anomaly. |

The surface ladder stays exactly as it is: `--bg` / `--surface-column` / `--surface-card` /
`--surface-card-hover`, hairline borders, no drop shadow at rest. `--shadow-float` is the only
shadow token in the system, defined once in `tokens.css`. The invariant is that single
definition, not a consumer cap — a second, independently-defined shadow value is the regression,
not an additional consumer of the one token. Measured today it is consumed at seven call sites: the
card drag overlay (`CardView.tsx:171`), the selection bar (`SelectionBar.tsx:29`), the search
results listbox (`SearchBox.tsx:321`), the carousel search overlay (`SearchBox.tsx:400`), the
move-to picker (`MoveToPicker.tsx:97`), the multi-select dropdown (`MultiSelect.tsx:248`), and the
modal (`Modal.tsx:110`) — corrected from this table's earlier three-element count, which predated
Phase 84 shipping the picker and the carousel overlay as additional consumers. At-rest cards
keep no shadow, confirmed via live measurement that an at-rest card's own `boxShadow` computes
to `none` at every breakpoint. Columns do not: Phase 115's live elevation check found
`Column.tsx`'s `[data-column]` root carries a permanent, unconditional
`inset 0 1px 0 rgba(255,255,255,0.02)` `boxShadow` (a hairline top-edge highlight) at every
breakpoint, regardless of drag or refusing state. This is not `--shadow-float`, not a second
definition of the single shadow token, and not an outward drop shadow that could suggest
floating elevation; it is a real, permanent, non-`none` `boxShadow` on a resting surface that
this row's earlier "columns keep no shadow" claim never actually verified, only ever having
confirmed a resting card. **Disposition (Phase 116 audit, KI-01): ACCEPTED, not a violation.**
Read correctly, "columns keep no shadow" is a claim about the ladder's own no-rest-drop-shadow
rule, and this value is not a drop shadow: it is a 1px INSET highlight with no blur and no
outward extent, categorically distinct from `--shadow-float` (a 6px-offset, 16px-blur,
zero-spread outward drop shadow, `0 6px 16px rgba(0, 0, 0, 0.45)`) and not a second definition
of that single token. Removing it would be a visual
regression to a landed, deliberate top-edge treatment with no reported visual defect and no
research basis for a different value, which an audit has no mandate to author on its own
initiative. `scripts/panel-115.mjs`'s `CHECKS.elevation` continues to assert the column's
`boxShadow` against this exact measured baseline as a regression guard, now guarding an
accepted value rather than tracking an open one.

The elevation ladder ranks RESTING surfaces only. The pressed state (see `pressed-formula` under
`## State palette`) is a transient darken from an element's own resting tier and does not enter
this ladder: a pressed card sits below its own resting `--surface-card` tier in luminance, not
below `--surface-column`, since the darken is always relative to the element's own starting
background, never a fixed rank position.

## Contrast floor

| Token or rule                  | Value                                            | Checked at                                          | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------ | ------------------------------------------------ | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `contrast-text-floor`          | 4.5:1                                            | n/a, viewport-independent; computed from tokens.css | Measured by `scripts/contrast-113.mjs` against every text-role token on all four surface tiers. Held everywhere; the instrument's residual allowlist is now permanently empty by design, see the closing paragraph below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `contrast-nontext-floor`       | 3:1                                              | n/a, viewport-independent; computed from tokens.css | Measured by `scripts/contrast-113.mjs` against every non-text-role token (accent, prio, col, status families) on all four surface tiers. Held on every pair; lowest measured is `--prio-low` / `--col-done` on `--surface-card-hover` at 3.32:1 (lowest on a resting tier: 3.56:1 on `--surface-card`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `contrast-destructive-text`    | 4.82:1 worst case, above the 4.5:1 floor         | n/a, viewport-independent; computed from tokens.css | LANDED, Phase 115. `--destructive-text` (`#ea6d71`, a white 20% lighten of `--destructive`) replaces `--destructive` at all 17 text call sites; its worst-case background is not a ladder tier but the real Lost-chip tint (`color-mix(in srgb, var(--destructive) 16%, var(--surface-card))`, `#3a2226`). Full 8-background ratio table in `115-MEASUREMENTS.md`'s `## Contrast` section. Continuously guarded since the Phase 115 review fix (WR-02): the tint pair is generated in `scripts/contrast-113.mjs`'s default derived-pairs list, re-derived from the live token values on every run (tied to `CardView.tsx:149`), so a tint-percentage bump or a `--destructive`/`--surface-card` retune that drops this worst case below floor now trips the instrument. The chip background is one opaque computed color pinned to the resting `--surface-card` tier; it does not re-base when the card behind it hovers, and deliberately so: at 16% over `--surface-card-hover` the mix (`#40272c`) would measure 4.48:1 against `--destructive-text`, below the floor, so any future re-base of the tint onto the hover tier must change the tint formula and the derived-pairs list, never land silently. |
| `contrast-danger-button-label` | 5.62:1 rest, 4.62:1 hover, above the 4.5:1 floor | n/a, viewport-independent; computed from tokens.css | LANDED, Phase 115 review fix (WR-01). The danger `Button` renders a white `#ffffff` label on its own fill; on the former fill (`--destructive`, `#e5484d`) this measured 3.91:1 at rest and 3.38:1 on the 12% white hover lighten, both below the text floor and invisible to the generated pair set (the label is a literal, not a token, and the fill is not a ladder tier). The fill split to `--destructive-button-fill` (`#c03136`, same 358deg hue family, darkened); `--destructive` itself is unchanged for non-text signals. Both the rest and hover pairs are generated in `scripts/contrast-113.mjs`'s default derived-pairs list, so this class of pair can no longer ship unchecked.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

`scripts/contrast-113.mjs` is the instrument that measures this floor: a zero-dependency Node
script that parses every color token in `tokens.css`, generates the full background x foreground
pair set plus a small derived-pairs list (real rendered text backgrounds that are not raw tokens:
the danger-button fill at rest and hover, the Lost-chip tint), and exits non-zero on a new failing
pair, on a recorded residual that regressed, or on a residual entry that went stale (a pair now
passing, or a pair no longer generated). This floor is met on every generated pair today,
derived pairs included: Phase 115 closed all three former destructive-text residuals at the value
level by splitting `--destructive` into a fill-role token (governed by the 3:1 non-text floor
above) and the text-role `--destructive-text` token (governed by the 4.5:1 text floor above), and
its review fix (WR-01) closed the one rendered text pair the generated set could not see, the
danger `Button`'s white label on the destructive fill, by splitting `--destructive-button-fill`
(see the `contrast-danger-button-label` row above); the
instrument's `RESIDUALS` allowlist is now permanently empty by design, proven live via a
reintroduction leg that hard-fails at the exact former residual ratios rather than silently
absorbing them, and any future failing pair is a hard failure with nowhere to hide.

## Motion

**SUPERSEDED BY v3.4: "Nothing changes" no longer holds.** This budget replaces the v2.9 flat
150ms `ease-out` shared by every transition with the named durations and easing tokens below.

| Token or rule              | Value                                                                                                                                                                                                                        | Checked at                                                                   | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `motion-panel-open`        | 200ms `ease-out`                                                                                                                                                                                                             | >=1600px, 1024-1599px, 768-1023px, <768px                                    | Today's shared flat 150ms splits into a distinct open value, triggered by a real trusted click opening the panel on a seeded card. Computed `transitionDuration` read `0.2s` at every breakpoint; observed elapsed 182.4ms to 198.2ms across breakpoints, an honest instrumentation margin under the requested value present at every candidate. Judged clean at >=1600px (widest travel, 560px panel) and <768px (full-screen cover): 150ms read abrupt, 240ms started reading laggy. Matches CONTEXT.md's own 200ms candidate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `motion-panel-close`       | 150ms `ease-in`                                                                                                                                                                                                              | >=1600px, 1024-1599px, 768-1023px, <768px                                    | Exit faster than enter, confirmed by measurement (150ms close vs 200ms open), triggered by a real trusted Escape keypress. Computed `transitionDuration` read `0.15s`; observed elapsed 133.3ms to 149.9ms.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `motion-card-move`         | 150ms `ease-out`                                                                                                                                                                                                             | >=1600px, 1024-1599px, 768-1023px, <768px                                    | Starting point is no motion today: `Board.tsx`, `CardView.tsx` and `Column.tsx` carry zero `transition`/`animation` declarations for card movement between columns. This is a runtime prototype measurement only, applied via inline style to a live card, never a file edit; Phase 114 owns building the real column-move mechanism (for example a FLIP-style transform) as a CSS transition, so the `reduced-motion-mechanism` kill-switch below reaches it. Computed `transitionDuration` read `0.15s`; observed elapsed 131.2ms to 133.4ms. At 768-1023px and <768px the carousel layout has no side-by-side column rect, so the recorded displacement is the `todo` column's own rendered width instead, the swipe-distance equivalent of a column move, not directly comparable to the wider breakpoints' side-by-side gap.                                                                                                                                                               |
| `motion-count-change`      | 120ms `ease-out`, animating `transform: scale()` (1 to 1.18), not `background-color`                                                                                                                                         | >=1600px, 1024-1599px, 768-1023px, <768px                                    | Starting point is no motion today: `Column.tsx`'s count-chip span re-renders its text content with zero transition. Runtime prototype only, same file-edit-free technique as `motion-card-move`; Phase 114 owns building the real mechanism (likely a `useEffect`-driven one-shot toggle) as a CSS transition on `transform`. A scale pulse was chosen over a background-color flash since the chip is small and a color flash on that small a target reads ambiguously as a state change. Computed `transitionDuration` read `0.12s`, observed elapsed 116.7ms to 116.9ms.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `easing-enter`             | `ease-out`                                                                                                                                                                                                                   | >=1600px, 1024-1599px, 768-1023px, <768px                                    | Kept value, not a retune: only `ease-out` exists in the codebase today, used for both enter and exit indiscriminately. A tightened no-overshoot `cubic-bezier(0.32, 0, 0.67, 0)` alternative showed no legibility improvement in the captured evidence; CONTEXT.md's own candidate list names `ease-out` for enter/hover explicitly. Computed `transitionTimingFunction` read `ease-out` verbatim.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `easing-exit`              | `ease-in`                                                                                                                                                                                                                    | >=1600px, 1024-1599px, 768-1023px, <768px                                    | New token: `ease-in` appears nowhere in `src/web/` today; every existing transition uses `ease-out` for both directions. Computed `transitionTimingFunction` read `ease-in` verbatim on the panel-close `transform` transition. Phase 114 must add this token; existing sites sharing the single `ease-out` for both directions (`Modal.tsx`, `ActivityDrawer.tsx`, `CardTimeline.tsx`, `SettingsScreen.tsx`) should adopt it on their own exit transitions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `reduced-motion-mechanism` | every motion in this budget is a CSS transition or CSS animation, so `tokens.css`'s existing `prefers-reduced-motion` block (lines 193 to 198) remains the whole mechanism; no new reduced-motion handling is added anywhere | n/a, viewport-independent; re-verified at >=1600px and <768px only, see Note | The reduced-motion query and the width queries are independent axes; the widest and narrowest breakpoints both reading `0s` establishes the block applies uniformly, so 1024-1599px/768-1023px add no new information. Re-applying all five landed motion values (panel open, panel close, card move, count change, `hover-transition`) under emulated `prefers-reduced-motion: reduce` read `transitionDuration: 0s` at both breakpoints. **The limit, verified not asserted**: a real `Element.animate()` (Web Animations API) call, 400ms, run under the same emulated state, resolved `playState: "finished"` at both breakpoints, proving the CSS-only kill-switch does not reach a WAAPI animation or a `requestAnimationFrame` loop. Phase 114 MUST implement every one of this budget's new/split motions as a plain CSS `transition` or CSS `@keyframes` `animation`, never `.animate()` or `requestAnimationFrame`, or the block would silently fail to zero it under reduced motion. |

No springs, no bounce: motion stays budgeted, not decorative. The table above is what each named
interaction gets, not a license to add more; a future motion absent from this table is a new
proposal for this document, not an assumed extension of it.

## Chrome dimensions

| Token or rule                          | Value                                             | Checked at                                | Note                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------------------------------- | ------------------------------------------------- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SyncStrip height — `--strip-height`    | 56px (wide/mid) / 44px (narrow)                   | >=1600px, 1024-1599px, 768-1023px, <768px | Responsive, not flat. A flat 56px research candidate costs a usable board card row on a real narrow-viewport device (the narrowest breakpoint, below 768px) — measured on a 844px-tall device preset, the row count drops from 6 to 5 at the very first candidate above 44px, with only about 1px of real headroom above today's value. The same 56px height costs zero rows at the three wider breakpoints (>=1600px, 1024-1599px, and 768-1023px), measured device presets: 9→9 rows, 8→8 rows, 6→6 rows respectively — so the strip grows at those three and stays at 44px only below 768px. The narrow viewport's added chrome presence therefore comes from horizontal padding and zoning, not height and not the wordmark — height there is constrained by card-row density, and the row-count measurement is the reason, while the wordmark is not rendered in the strip below 768px at all (see the `wordmark` row). What carries identity there is the `Glyph`, which stays at full size and keeps its place as the leftmost element of the zone grid.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Horizontal padding — `--strip-padding` | 24px (wide/mid) / 16px (narrow)                   | >=1600px, 1024-1599px, 768-1023px, <768px | Responsive, not flat — both values sit inside the researched 16px to 24px range. A flat 24px was measured to put two strip controls past the viewport edge at 390px wide: Activity ended 14.53px past it and Settings 46.53px past it, for 47px of horizontal overflow on the strip itself, rising to 61px under the longer failed-poller status strings. That overflow is pre-existing, not a regression — the pre-revamp commit measured 61px against the shipped 60px under identical status text — so raising padding 16px to 24px did not cause it and lowering it alone does not close it. The narrow step to 16px returns 16px of that budget and is one part of the fix, not all of it — the strip's remaining narrow-width behaviour is recorded in `docs/ARCHITECTURE.md#app-shell-zones`. Defined once in `tokens.css`. **Scope, decided in Phase 89 rather than left to silence: this row governs every full-width chrome row above the board, not the SyncStrip alone.** Phase 89 measured both chrome rows in the same DOM at the same instant and found their heights matching at all four breakpoints (56/56/56/44, both from `--strip-height`) while `UpdateBanner`'s content sat 8px inboard of the strip's at the three wider ones — 16px (`--space-lg`) against 24px — because it had adopted the height token but not the padding token. At the narrowest breakpoint the delta read 0 only because `--space-lg` coincidentally equals the narrow `--strip-padding`, so the misalignment was invisible exactly where the two rows are least likely to be seen together. `UpdateBanner`'s `rowStyle` now consumes the token too, so there are two consumers: `SyncStrip.tsx`'s `stripContainerStyle` and `UpdateBanner.tsx`'s `rowStyle`. A chrome row that deliberately wants a different inset is a recorded finding, never an ad-hoc literal. |
| Wordmark size/weight — `wordmark`      | 22px / 600, not rendered in the strip below 768px | >=1600px, 1024-1599px, 768-1023px, <768px | Replaces the hardcoded `fontWeight: 800` formerly duplicated across `SyncStrip.tsx` and `App.tsx` with `--font-display` (22px) at `--weight-semibold` (600) — a real type-system value defined once instead of an ad hoc literal copied across two files. Confirmed not clipped and not crowding the New Ticket control cluster at the three breakpoints that render it. Below 768px the strip renders the `Glyph` alone and omits the wordmark span entirely: at 136.5px the wordmark is over a third of a 390px viewport, and the strip's fixed elements do not fit beside it there even with the status text erased. The app name stays in the accessibility tree through the glyph's `title`/`aria-label`. `wordmarkStyle` is unchanged and its other consumers (`App.tsx`, `FirstRunSetup.tsx`) render it at every width.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

View-switch pattern — `view-switch`: one visually contained segmented mode control with a shared
background and the accent on the active segment. No candidate was prototyped this phase — only a
baseline capture of today's two-separate-icon-buttons rendering was taken, per the locked decision
that exact rendering is a Phase 85 product-visual-prototype question, not a value this contract
fixes. Resolved in Phase 85: see `## Deferred decisions` row 2 for the chosen rendering and its
rendered-evidence reason. Retuned later in Phase 85 (same row) from an opaque fill to an accent
tint after a UI review found the opaque fill outweighed New Ticket's own hierarchy.

## Card identity

Per-column visual identity is limited to attention only: Needs Input gets a distinguishing
treatment; every other column's cards stay visually terse. The board's core value is reliably
showing which tickets need human attention, and per-column identity everywhere would spend
scanning density on information the column header already carries.

## Component library — DECISIVE

**Ratified:** keep the hand-rolled `tokens.css` plus inline `style` object approach. Tailwind and
shadcn are not adopted for this milestone.

Three reasons. First, every audit finding behind this milestone is a design decision that was
never made — an inconsistent radius, an under-used weight tier, a copy-pasted shadow — rather than
a limitation of the delivery mechanism; Linear, Raycast, and Warp all express their own systems as
bespoke CSS custom properties too, so the mechanism was never the gap. Second, shadcn's own
unmodified defaults carry a recognizable generic signature of their own, so adopting it risks
trading one generic look for another rather than buying a genuinely distinct one. Third, migrating
every component in `src/web/primitives/` and `src/web/features/` for zero functional change is the
same diff-churn trade `frontend-design-system.md`'s `## Styling approach — DECISIVE` section
already rejected for CSS Modules, vanilla-extract, and styled-components — made larger here, and in
a repo with no test code, where verification is a smoke-tester exercising the running app rather
than a suite that could catch a regression automatically.

Honest caveat: a future milestone that wants a genuinely different visual language —
illustration, marketing surfaces, a public-facing site — would make Tailwind defensible. This
ratification is scoped to the current milestone and is not permanent.

## What this does NOT change

This section exists so later phases cannot quietly expand scope beyond what this contract fixes.

- The terminal.
- Motion (see `## Motion` above). **SUPERSEDED BY v3.4: `## Motion` now carries a named budget;
  see that section's table.**
- The surface-ladder elevation approach (see `## Elevation` above). Amended, not superseded: the
  ladder itself (`--bg` / `--surface-column` / `--surface-card` / `--surface-card-hover`, ranked by
  lightness, no rest shadows) is unchanged, `## Elevation`'s new pressed-state prose only clarifies
  that a transient pressed darken does not enter the ladder.
- The semantic `--prio-*`, `--col-*`, and `--status-*` palettes.
- All behavior.
- The mobile carousel.
- Touch drag activation constraints.
- Coarse-pointer resize handles.
- The detail panel's history-based back-button takeover on narrow viewports.
- The no-spinner optimistic board contract.
- Column and panel resize with persisted widths.
- The existing aria wiring, including `aria-expanded`, `aria-live="polite"` on sync status, and
  `aria-pressed` on the view toggles.
- The focus-visible-only gating of focus rings.
- The `Notice` / `Field` / `Modal` primitive contracts — their props, structure and behaviour. This
  exemption is about the primitives' API, not a freeze on the tokens they resolve: a primitive whose
  computed value diverges from a table above is still a finding, and Phase 89 fixed one such
  divergence inside `Notice` without changing a single prop (see the `mono-sizing-rule` row).
- The existing scroll-gutter and `::-webkit-scrollbar` handling in `tokens.css`.

## Deferred decisions

| Question                                                                                                                  | Decision or deferral                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Owning phase  |
| ------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- |
| 1. Exact final pixel values                                                                                               | ANSWERED — fixed in the value sections above, checked at the four breakpoints.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | This document |
| 2. Board/Orca view switch rendering                                                                                       | ANSWERED — Candidate C shipped in Phase 85, then retuned in a Phase 85 gap-closure pass after the UI review found it inverted the strip's own hierarchy goal: a fully opaque `var(--accent)` fill on the 24×28px active segment out-weighed New Ticket, the control the phase set out to elevate as "the single most-used control in the strip." The active segment now takes a `color-mix(in srgb, var(--accent) 16%, var(--surface-column))` tint background with `var(--accent)` icon color — the same 16% tint already used by the inbox count badge — so the accent still marks the active segment (still "active view toggle," unchanged accent job) at a weight below New Ticket's contained treatment. See `## Chrome dimensions`'s `view-switch` paragraph and `docs/ARCHITECTURE.md#app-shell-zones`. | Phase 85      |
| 3. Per-column visual identity on cards                                                                                    | ANSWERED — attention only, per `## Card identity`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | This document |
| 4. Whether the radius vocabulary reaches utilitarian elements                                                             | ANSWERED — no; the vocabulary assigns neither the resize handles nor the scrollbar thumb a bucket. Corrected in Phase 89: "stay undecorated" overstated it, because the thumb already inherits a measured 6px `var(--radius)` from the app-wide scrollbar block. See the `radius-scope` row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | This document |
| 5. Exact SyncStrip height                                                                                                 | ANSWERED — responsive rule, 56px at wide/mid breakpoints and 44px below 768px, with the measured row-count justification, per `## Chrome dimensions`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | This document |
| 6. Formalizing the focus-versus-selection rule                                                                            | ANSWERED yes — the rule is written above in `## Color roles` and lands in `frontend-design-system.md` during Phase 84, alongside the primitive changes that implement it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Phase 84      |
| 7. Modal heading cascade                                                                                                  | ANSWERED — uniformly to all eight `Modal.Header` call sites (`StartModal`, `CleanupModal`, `SettingsModal`, `CreateTicketModal`, `PlaybookEditorModal`, `GroupStartModal`, plus the nested `PlaybookDeleteConfirm` inside `SettingsModal.tsx`, which the original "six modals" count missed). Measured identical at all eight sites, at four breakpoints, by `88-BASELINE.md`'s `## Heading cascade audit`; no per-modal tune was needed. Any modal that genuinely needs different weight becomes a recorded finding, never an ad-hoc tweak.                                                                                                                                                                                                                                                                    | Phase 88      |
| 8. `font-feature-settings` (`ss03` / `cv01` / `calt`)                                                                     | PRE-EMPTED, not declined. `tokens.css:43` names Inter first in the font stack, but there is no `@font-face`, no webfont link in `src/web/index.html`, and no bundled font file, so the app renders in the OS system font on every real machine, and those tags are Inter-specific with no defined effect on San Francisco or Segoe UI. This contract specifies no `font-feature-settings` value because it would do nothing.                                                                                                                                                                                                                                                                                                                                                                                    | This document |
| 9. Whether to load Inter as a real webfont                                                                                | DEFERRED — the question the Inter finding above actually raises. Scoping it needs: bundle size, licensing, and FOUT/FOIT behavior. If declined, the decline is recorded rather than silently dropped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Phase 84      |
| 10. Needs-attention vs. multi-selected card ring collision                                                                | ANSWERED — there is no collision to resolve. Phase 89 measured the two rings live at all four breakpoints: needs-attention is `--accent` (`rgb(94, 106, 210)`), multi-selected is `--text` (`rgb(232, 233, 234)`), and `CardView.tsx` composes them in an `else if`, so no card can carry both. This row and the `## Color roles` Accent row both described a state the code did not hold; the deferral rested on an unchecked source reading, not a measurement. Nothing is queued.                                                                                                                                                                                                                                                                                                                            | Phase 89      |
| 11. Mono treatment for the modal heading's new 17px size                                                                  | ANSWERED — the mono one-step-down sizing rule does NOT apply to the modal heading. That rule was checked against the heading's original 12px baseline, where mono sat beside body text; a dialog title has no adjacent UI text to sit below, and stepping it down to 15px would re-quiet the exact thing criterion 1 exists to fix. The heading stays at the full `--font-heading` (17px) value and keeps `--font-mono` (`88-CONTEXT.md`'s deferred-decision-11 resolution).                                                                                                                                                                                                                                                                                                                                    | Phase 88      |
| 12. `--destructive` text contrast residual                                                                                | ANSWERED, closed by Phase 115. Split `--destructive` into an unchanged fill-role token (fills, borders, icon glyphs and status dots, governed by the 3:1 non-text floor) and a new text-role `--destructive-text` token (`#ea6d71`, a white 20% lighten), repointing all 17 text call sites while leaving the 6 non-text call sites on the unchanged fill token. `scripts/contrast-113.mjs` now reports 0 failing and 0 residual across 75 pairs (72 generated plus the 3 derived pairs the Phase 115 review fix added), and a reintroduction leg (a mkdtemp copy setting `--destructive-text` back to the old fill hex) hard-fails at the exact former residual ratios, 4.40 and 4.11, proving the tripwire is live rather than silently absorbed.                                                             | Phase 115     |
| 13. Card keyboard focusability                                                                                            | ANSWERED, closed by Phase 114. `Card.tsx`'s composed pointer/focus handlers now route the card's keyboard focus through `focusRing()`'s governed `2px solid var(--accent)` ring instead of the browser's ungoverned native outline; confirmed live by a real Tab traversal reaching the card at every breakpoint (`node scripts/panel-114.mjs --check board-states`, `114-MEASUREMENTS.md`'s `## After` > `### States` table, "card focus (keyboard) outline"). The coverage rule now fully reaches the card.                                                                                                                                                                                                                                                                                                   | Phase 114     |
| 14. Resize-handle state distinguishability                                                                                | ANSWERED, closed by Phase 114. `Column.tsx`'s resize handle now splits keyboard focus onto `focusRing(handleFocused, true)` (`2px solid`, `0` offset, flush), distinct from the border-based `hover-resize-handle`/dragging treatment; confirmed live (`node scripts/panel-114.mjs --check board-states`, `114-MEASUREMENTS.md`'s `## After` > `### States` table, "resize handle keyboard focus outline"). Hover, focus and dragging no longer resolve to one treatment.                                                                                                                                                                                                                                                                                                                                       | Phase 114     |
| 15. Flat-150ms retirement's two non-token-mapped sites (`CardTimeline.tsx` chevron, `PlaybookEditorModal.tsx` static box) | ANSWERED, closed by Phase 114 (Plan 07). `CardTimeline.tsx`'s disclosure chevron has no contract row (no token exists for a disclosure toggle); it keeps `--motion-panel-close`'s numeric value for both directions and swaps only the easing by the `expanded` boolean, a recorded naming compromise, not a new token. `PlaybookEditorModal.tsx`'s flat-150ms declaration was DELETED, not migrated: the container only ever mounts/unmounts via `{generateOpen && (...)}`, never toggles `opacity`/`transform` on a persistent node, so nothing could ever have animated through it; the site never needed a motion token. Both adjudicated per `114-MEASUREMENTS.md`'s own `## Deviations` entry naming them.                                                                                                | Phase 114     |

## How values were checked

Every value above was checked against real rendered content, not derived on paper. An isolated,
disposable instance of the app was seeded with realistic cards — short and long titles, several
with full multi-paragraph markdown descriptions, spread across all six board columns — and booted
independently of the live board. Each candidate value was applied as a runtime CSS custom-property
override against the real running app (never a source edit), then viewed at four breakpoints
(>=1600px, 1024-1599px, 768-1023px, and below 768px on named real-device viewport presets) against
real card titles, real panel copy, and real modal copy, with screenshots and, for interaction-gated
values like the focus ring, real keyboard and pointer input rather than synthetic focus calls. The
row-count measurements behind the SyncStrip height read every input — viewport height, strip
height, column-header height, card height, and the inter-card gap — live from the rendered page,
never as a hardcoded literal. Wherever a checked value disagreed with a starting candidate, the
checked value won and the disagreement is recorded above as a one-line deviation note.

**v3.4 method.** The same isolated, seeded, disposable instance method, this time on port 47887
under a `/tmp` sandbox `HOME` with a placeholder Linear key. A blocking preflight asserted zero
live tmux, ttyd and cloudflared processes before every boot, because the booted instance sweeps
machine-wide. Every candidate was applied as a runtime custom-property or inline-style override,
never a source edit, with the working tree asserted clean at the end of every measurement task.
The same four breakpoints were used, with the named BP-D device preset (iPhone 12 Pro metrics,
driven via raw CDP where the Chrome DevTools MCP tools were not registered this session). Durations
were proven numerically by observed `transitionrun`/`transitionend` timestamps, not by eye, with
visual frames captured at a stated slow-down multiplier (commonly 10x) proving shape only, never
the timing claim itself. Hover and focus states at the narrowest breakpoint were recorded as
rendered-state checks rather than pointer-driven ones, since that device preset's `mobile:true`
metrics carry no hover input; a real Tab traversal and real pointer sweeps were reserved for the
two wider breakpoints. The contrast floor was computed by `scripts/contrast-113.mjs`, a
break-proven, zero-dependency instrument. As in v2.9, wherever a checked value disagreed with a
starting candidate, the checked value won and the disagreement is recorded above as a one-line
deviation note.

Phase 115 extended this method with live rendered-surface reads, never runtime custom-property overrides, driven by a break-proven `scripts/panel-115.mjs` harness that mutates and restores real source to prove each check's own negative control.
